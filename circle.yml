machine:
  ruby:
    version: 2.3.0
    
test:
  override:
    - bundle exec rake minitest test
    - mkdir -p $CIRCLE_TEST_REPORTS/minitest
    - cp $PWD/test/reports/TEST*.xml $CIRCLE_TEST_REPORTS/minitest